# Web 框架
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# 配置 / 数据校验
pydantic==2.6.4
pydantic-settings==2.2.1

# ORM（开发期 SQLite，生产 PostgreSQL+pgvector）
SQLAlchemy==2.0.29

# 鉴权（密码哈希用标准库 pbkdf2，无需 bcrypt 原生依赖）
PyJWT==2.8.0

# 调用云端大模型（DeepSeek，OpenAI 兼容协议）
httpx==0.27.0

# 本地嵌入（开发期）；生产可切换 DashScope/Qwen 云端 Embedding
fastembed==0.2.6

# 文档解析（PDF -> 文本）
PyMuPDF==1.24.1
